# Environment and local config
.env

# macOS metadata
.DS_Store

# Python cache
__pycache__/
*.pyc

# Local data outputs
data/*.xlsx
data/*.numbers
data/*_run_report.json
data/*.sqlite
data/*.sqlite-journal
data/*.sqlite-wal
data/*.sqlite-shm

# Laravel storage path the dispatcher writes to in production; if the cron
# script ran locally during dev/testing it created these dirs — don't track.
storage/

# Local copies from droplet (not part of this repo)
divento-scrapper/
diventoscrapper/

# Local runtime logs
logs/
