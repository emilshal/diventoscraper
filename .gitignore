# Environment and local config
.env

# macOS metadata
.DS_Store

# Python cache
__pycache__/
*.pyc

# Local data outputs
data/*.xlsx
data/*.numbers
data/*_run_report.json

# Local copies from droplet (not part of this repo)
divento-scrapper/
diventoscrapper/

# Local runtime logs
logs/

# Claude Code tooling (memory, agent journals, worktree state)
.claude/

# Local artifacts pulled off the compromised droplet for forensics/audit.
# Contains old .env files (now-rotated secrets), DB dumps, forensics bundle,
# and the quarantined malware. NEVER commit.
untrusted-from-compromised-box/
